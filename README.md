```python
# SYSTEM BREACH PROTOCOL : ACTIVE
# Target: lynniethecool
# Status: COMPROMISED

import time
import sys
import os

def log(text, delay=0.04):
    for c in text:
        sys.stdout.write(c)
        sys.stdout.flush()
        time.sleep(delay)
    print()

log("[>] Connecting to ROBLOX mainframe...")
time.sleep(1)
log("[✓] Session established.")
log("[>] Injecting Trojan script... [IN PROGRESS]")
time.sleep(1)
log("[✓] Exploit deployed successfully.")

log("\n[!] Bypassing 2FA security layer...")
time.sleep(1)
log("[✓] 2FA bypassed.")
log("[✓] Admin privileges granted.")

log("\n[>] Extracting user data...")
time.sleep(0.5)
log("    - Username     : lynniethecool")
log("    - Email        : RESTRICTED")
log("    - Robux        : 999,999,999")
log("    - Password     : ********")
log("[✓] Data extraction complete.")

log("\n[>] Activating remote surveillance tools...")
log("    - Webcam       : ACTIVE")
log("    - Microphone   : ACTIVE")
log("    - Location     : TRACKING ENABLED")

log("\n[WARNING] Unauthorized access detected.")
log("[ACTION] Triggering countermeasure: Digital Identity Erasure")

time.sleep(1)
log("\n[>] Formatting user's existence...")
log("    - Socials      : DELETED")
log("    - Inventory    : PURGED")
log("    - Friends      : ERASED")
log("    - IP Address   : SPOOFED")
log("    - Legal Identity: NULLIFIED")

time.sleep(2)
log("\n[✓] Subject 'lynniethecool' has been wiped from all known systems.")
log(">> CONNECTION TERMINATED <<")
